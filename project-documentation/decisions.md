- 2026-02-18: [review-reconciler] Accepted pushback on path traversal hardening for `websiteName` output paths. Guidance: do not flag missing sanitization when input is trusted, hand-edited local `sites.json` for this personal dev extractor workflow. Scope: `runner.js`, `lib/writer.js`, local-only execution.
- 2026-02-18: [review-reconciler] Accepted pushback on missing test findings by default. Guidance: do not raise missing-test findings unless tests are explicitly requested or a change introduces concrete correctness/safety risk that requires test coverage to validate. Scope: repository-wide code review in this project.
- 2026-02-18: [review-reconciler] Accepted pushback on screenshot sampling and infinite-scroll heuristics in color extraction. Guidance: do not flag >1 viewport scroll steps or one-growth infinite classification when used intentionally to prioritize broad page coverage and bounded extraction cost in this workflow. Scope: `extractors/color-system.js` review context for this project.
- 2026-02-19: [review-reconciler] Accepted pushback on null-check requirement for tablist listener binding. Guidance: do not flag `document.querySelector('[role="tablist"]')` listener attachment as nullable when the same template unconditionally renders the `role="tablist"` element. Scope: `server/render.js` `renderSite` output structure.
- 2026-02-19: [review-reconciler] Accepted pushback on per-site category counts for home cards. Guidance: do not flag `registry.length` card metadata as a defect in the local dev browser unless per-site completeness becomes a stated product requirement. Scope: `server/render.js` `renderHome`, local tooling UX.
- 2026-02-20: [review-reconciler] Accepted pushback on attributing pre-existing `sites.json` diffs to the current developer response. Guidance: when reconciliation context indicates a change predates the addressed patch, do not keep it as an active finding against that patch without provenance. Scope: working-tree reviews with mixed pre-existing and new edits.
- 2026-02-20: [review-reconciler] Accepted pushback on broad cookie-dismissal selectors. Guidance: do not flag unscoped `cookie-accept`/`consent-accept` selector usage as a defect when extraction runs against trusted, user-curated site lists and existing visibility/navigation safeguards are intentional tradeoffs for coverage. Scope: `lib/browser.js` cookie-banner dismissal in this local extractor workflow.
- 2026-02-20: [review-reconciler] Accepted pushback on nth-index element targeting fragility in interaction state extraction. Guidance: do not flag two-pass `selector + selectorIndex` targeting as a defect in this workflow when page warmup precedes extraction and interactions run immediately without navigation; prefer escalation only with concrete evidence of mis-targeting. Scope: `extractors/interaction-states.js` discovery/capture path.
- 2026-02-21: [review-reconciler] Accepted pushback on Playwright `Page` recovery-depth field mutation for cookie-dismiss recursion control. Guidance: do not flag `page._dismissalRecoveryDepth` usage as a defect in this local, non-library extractor workflow absent concrete evidence of conflicts or runtime issues. Scope: `lib/browser.js` cookie-banner recovery path.
- 2026-03-04: [review-reconciler] Accepted pushback on fixed initial seed count assertions in ordered manual test plans. Guidance: do not flag expectations like "exactly N seeded records" as defects when prerequisites require a fresh server/process state and the count check is intentionally first. Scope: manual API test plans for `sites/hermes-video-gen/backend` task docs.
- 2026-03-04: [review-reconciler] Accepted pushback on duplicate mock token constants in tiny prototype auth flows. Guidance: do not flag two-file duplication of a fixed mock token as a defect when scope is intentionally small and both definitions remain aligned; escalate only if divergence occurs or reuse expands. Scope: `sites/hermes-video-gen/backend/routes/auth.js`, `sites/hermes-video-gen/backend/middleware/auth.js`.
- 2026-03-04: [review-reconciler] Accepted pushback on additional multipart library error-path test expectations. Guidance: do not flag missing app-level tests for framework/library-managed multipart failures by default in this project when core flow tests exist and no concrete app-specific failure handling was added. Scope: `sites/hermes-video-gen/backend` API route reviews.
- 2026-03-04: [review-reconciler] Accepted pushback on placeholder redirects to future pages during sequenced feature tasks. Guidance: do not flag redirects to not-yet-implemented routes/pages as defects when task scope explicitly marks them as intentional placeholders in a staged build plan. Scope: `sites/hermes-video-gen/frontend/js/auth.js` and matching task documentation for the 10-task Hermes flow.
- 2026-03-04: [review-reconciler] Accepted pushback on mock-auth error redirects in prototype-only flows. Guidance: do not flag intentional "redirect on auth API error" behavior as a defect when prototype requirements explicitly prioritize forward navigation over error fidelity and the code is clearly documented as mock behavior. Scope: `sites/hermes-video-gen/frontend/js/auth.js` prototype auth handling.
- 2026-03-04: [review-reconciler] Accepted pushback on auth-guard throw after redirect in prototype dashboard flow. Guidance: do not flag `throw` immediately after unauthenticated redirect as a defect when it is intentionally used to hard-stop module execution and no active error monitoring pipeline exists; revisit only when monitoring requirements are introduced. Scope: `sites/hermes-video-gen/frontend/js/dashboard.js` auth guard path.
- 2026-03-04: [review-reconciler] Accepted pushback on duplicate image-type guards between file input and drag/drop in a prototype create form. Guidance: do not flag missing extra MIME guard in file input `change` handlers when `accept="image/*"` already constrains picker selection and drag/drop paths are explicitly validated. Scope: `sites/hermes-video-gen/frontend/js/create.js` upload handling.
- 2026-03-04: [review-reconciler] Accepted pushback on blob URL revocation in short-lived prototype media previews. Guidance: do not flag missing `URL.revokeObjectURL` calls by default in mock/prototype flows with expected short sessions unless repeated long-lived usage or measurable memory pressure is demonstrated. Scope: `sites/hermes-video-gen/frontend/js/create.js` preview lifecycle.
- 2026-03-05: [review-reconciler] Accepted pushback on classifying non-HTTP font-face sources as unavailable in local typography preview rendering. Guidance: do not flag `data:`/relative `src` entries being treated as non-loadable in this workflow unless there is concrete evidence of incorrect browser behavior for intended preview contexts. Scope: `server/render.js` font availability indicators in `renderTypographySection`/`getFontStatus`.
- 2026-03-06: [review-reconciler] Accepted pushback on requiring cross-face status prioritization for mixed-delivery font families. Guidance: do not flag first-match family status evaluation as a defect by default in this local typography workflow unless concrete extracted data shows mixed `src` delivery within one family causing incorrect availability badges. Scope: `server/render.js` `getFontStatus` family matching.
- 2026-03-06: [review-reconciler] Accepted pushback on protocol-relative font URL handling in external-source detection. Guidance: do not flag missing `//` URL support in `isExternalSrc` for this project unless extracted corpus data demonstrates protocol-relative `@font-face src` values causing incorrect availability status. Scope: `server/render.js` `isExternalSrc` and typography status indicators.
- 2026-03-06: [review-reconciler] Accepted pushback on per-site confinement checks for local font-file preview route. Guidance: do not flag `/fonts/:site/*` handlers that restrict traversal to the project `design-system` tree (without additional per-site `fontsRoot` confinement) as defects by default in this localhost-only tooling workflow unless sensitive data is introduced under that tree or exposure is no longer intentional. Scope: `server.js` font asset route path validation.
- 2026-03-06: [review-reconciler] Accepted pushback on requiring explicit downloaded-font markers in typography JSON. Guidance: do not flag `src.startsWith('fonts/')` classification/rewrite as ambiguous by default in this Playwright+Chrome local extractor workflow unless concrete extracted data demonstrates real collisions with non-downloaded `@font-face` sources. Scope: `extractors/type-system.js`, `lib/font-downloader.js`, `server/render.js` font status/CSS rewrite logic.
- 2026-03-06: [review-reconciler] Accepted pushback on escaping font metadata before `innerHTML` injection in typography alternative picker. Guidance: do not flag missing HTML escaping for maintainer-controlled local font catalog/DB values in this localhost-only tooling workflow unless input trust boundaries change or external/untrusted writes are introduced. Scope: `server/render.js` font alternative picker rendering (`renderAltSpecimen`, `renderCatalog`).
- 2026-03-06: [review-reconciler] Accepted pushback on flagging outlier typography token values in extracted output snapshots. Guidance: do not flag values like `fontSize: 0px` or very small `lineHeight` in `design-system/*/type-system/typography.{json,css}` as direct defects when files are extractor artifacts; instead treat as extraction-quality follow-up and recommend re-running/investigating capture conditions. Scope: extractor-generated typography outputs and related review findings.
- 2026-03-05: [review-reconciler] Accepted pushback on unbounded POST body accumulation checks for font alternative save endpoint. Guidance: do not flag missing payload-size limits on `/api/font-db/:site` as a defect by default in this localhost-only design tooling workflow with tiny JSON payloads unless exposure broadens beyond local trusted use or large-body abuse becomes plausible. Scope: `server.js` font DB POST handler.
- 2026-03-06: [review-reconciler] Accepted pushback on requiring explicit GET-only guards for read-only site/browser routes after enabling POST for API endpoints. Guidance: do not flag missing per-route method checks on non-mutating handlers (e.g., `/`, `/site/:name`, `/fonts/:site/*`) as defects by default in this localhost-only tooling workflow unless behavior contracts explicitly require strict 405 semantics. Scope: `server.js` top-level method gate and route handlers.
- 2026-03-06: [review-reconciler] Accepted pushback on treating `originalFont` object-key assignment as prototype-pollution defect in local font DB endpoint. Guidance: do not flag `db[originalFont]` key handling in `/api/font-db/:site` as a security defect by default in this localhost-only trusted tooling workflow unless untrusted/external request sources are introduced or object-key behavior causes demonstrated corruption. Scope: `server.js` font DB POST handler key assignment.
- 2026-03-06: [review-reconciler] Accepted pushback on aligning missing-site semantics between font DB GET and POST endpoints. Guidance: do not flag `GET /api/font-db/:site` returning `{}` while `POST /api/font-db/:site` can fail for nonexistent site directories as a defect by default when empty-read plus constrained-write behavior is intentional in this local tooling workflow. Scope: `server.js` font DB GET/POST route semantics, `server/font-db.js`.
- 2026-03-06: [review-reconciler] Accepted pushback on requiring inline save-failure UI for font alternative selection. Guidance: do not flag empty catch blocks or missing inline error banners in `selectFont` as defects by default in this localhost-only design-tool workflow when request failures are already prevented from causing incorrect specimen rendering. Scope: `server/render.js` font alternative picker save path.
- 2026-03-06: [review-reconciler] Accepted pushback on requiring CDN-failure fallbacks for third-party animation libraries in localhost demo pages. Guidance: do not flag missing runtime guards around CDN-loaded GSAP/ScrollTrigger by default when scope is local demo usage and offline/network-failure resilience is not a stated requirement. Scope: `demo/apple/shortform-ai/index.html` inline animation bootstrap.
- 2026-03-06: [review-reconciler] Accepted pushback on defensive parse guards for auth sessionStorage reads in local prototype flow. Guidance: do not flag missing `try/catch` around `JSON.parse(sessionStorage.getItem(AUTH_KEY) || '{}')` as a defect when the same module is the sole writer and always uses `JSON.stringify`, unless external/untrusted writers or migration compatibility requirements are introduced. Scope: `public/js/auth.js` auth state serialization/deserialization.
- 2026-03-06: [review-reconciler] Accepted pushback on broadening static-site link interception beyond explicit `/` and `./` patterns. Guidance: do not flag missing URL parsing or generalized same-origin detection in transition handlers by default when navigation targets are intentionally constrained to known controlled link formats in local prototypes. Scope: `public/js/main.js` internal-link transition interception.
- 2026-03-06: [review-reconciler] Accepted pushback on mobile-responsive defects for desktop-first nav prototype tasks. Guidance: do not flag missing mobile nav breakpoints as active defects when task scope/acceptance criteria explicitly defer mobile support; track as future responsive-pass work instead. Scope: `public/index.html`, `public/css/components/nav.css`, Task 194-style desktop-first component implementations.
- 2026-03-06: [review-reconciler] Accepted pushback on changing hero CTA href away from `/signup.html` in caspers-vid auth routing. Guidance: do not flag `/signup.html` CTA href as incorrect when flow intentionally uses it for logged-in users without a selected plan and JS branches logged-in users with plan to `/app/studio.html`. Scope: `public/index.html` hero CTA handler and `public/js/auth.js` plan/auth routing contract.
- 2026-03-06: [review-reconciler] Accepted pushback on requiring plan-selection gating in additional homepage CTAs. Guidance: do not flag gallery/final CTA handlers that route authenticated users directly to `/app/studio.html` when the task contract explicitly specifies that routing and downstream plan validation is intentionally delegated to studio/app flows. Scope: `public/index.html` `.js-gallery-cta` and `.js-final-cta` auth intercept handlers.
- 2026-03-06: [review-reconciler] Accepted pushback on placeholder `href="#"` social links in staged footer implementation. Guidance: do not flag temporary hash-link social anchors as defects when task scope explicitly marks them as placeholders pending future real profile URLs. Scope: `public/index.html` footer social links in prototype/staged tasks.
- 2026-03-06: [review-reconciler] Accepted pushback on footer hover token contrast in extracted reference styling. Guidance: do not flag low-contrast hover colors (for example `#202020` on `#111111`) as defects by default when they intentionally preserve extracted design-system tokens; raise only when accessibility conformance is an explicit requirement for the task. Scope: `public/css/components/sections.css` footer link hover states.
- 2026-03-06: [review-reconciler] Accepted pushback on requiring logged-in follow/save behavior beyond auth interception in Explore prototype task. Guidance: do not flag `Follow`/`Save` handlers that only call `interceptIfNeeded(...)` as defects when acceptance criteria are explicitly limited to auth interception wiring and no backend/app contract for logged-in action persistence is defined yet. Scope: `public/explore.html` follow/save button handlers for this task scope.
- 2026-03-06: [review-reconciler] Accepted pushback on hover-gated pointer-events in Explore card overlays. Guidance: do not flag `.card-overlay { pointer-events: none; }` with `.explore-card:hover .card-overlay { pointer-events: auto; }` as a defect by default when the interaction model intentionally restricts hidden overlay controls and mobile behavior is explicitly deferred to a dedicated design/responsive task. Scope: `public/css/components/explore.css` card overlay interaction gating in prototype Explore page tasks.
- 2026-03-06: [review-reconciler] Accepted pushback on placeholder nav links to not-yet-implemented pages in staged caspers-vid tasks. Guidance: do not flag `/about.html` or `/login.html` links as defects by default when the flow is explicitly mid-sequence, pages are planned for later tasks, and placeholder navigation is intentional. Scope: `public/pricing.html`, `public/signup.html`, staged caspers-vid frontend task progression.
- 2026-03-06: [review-reconciler] Accepted pushback on minimal stub signup validation in prototype auth flow. Guidance: do not flag `novalidate` plus silent early-return validation in signup stubs as defects by default when scope is flow demonstration with simulated auth and no requirement for production-grade form UX yet. Scope: `public/signup.html` prototype form handling.
- 2026-03-06: [review-reconciler] Accepted pushback on retaining `sans-serif` fallback tokens for required web fonts when explicit runtime diagnostics are implemented. Guidance: do not flag generic fallback presence alone as a defect when the page actively detects missing required fonts and shows a visible banner naming missing families, cause, and remediation so fallback is not silent. Scope: `public/pricing.html`, `public/signup.html`, `public/css/components/pricing.css` font-loading behavior.
- 2026-03-06: [review-reconciler] Accepted pushback on preserving `intendedAction` through signup in caspers-vid auth flow. Guidance: do not flag `signup.html` clearing intended action and always redirecting to `/app/studio.html` as a defect when task requirements explicitly define signup as a studio-first onboarding path and reserve intended-action resume behavior for returning-user login flows. Scope: `public/signup.html`, `public/login.html`, `public/js/components/explore.js` auth-return behavior.
- 2026-03-06: [review-reconciler] Accepted pushback on read-time validation of `intendedAction.returnUrl` in login redirect flow. Guidance: do not flag missing defensive checks around `getIntendedAction()`/`returnUrl` at login redirect as a defect by default when intended action is module-controlled session state in local prototype auth flows and malformed-state hardening is out of scope; prefer addressing integrity at write-time if requirements change. Scope: `public/login.html`, `public/js/auth.js` intended-action persistence/redirect contract.
- 2026-03-06: [review-reconciler] Accepted pushback on cross-device fallback requirement for `fs.renameSync` in scroll-recording artifact finalization. Guidance: do not flag missing `EXDEV` copy/unlink fallback by default when this standalone tool is explicitly scoped to local macOS development on a single APFS volume and the design doc intentionally keeps move logic minimal; revisit only if execution environments broaden beyond that assumption. Scope: `record-scroll.js` video finalization path (`os.tmpdir()` -> `extraction-assets/...`).
- 2026-03-06: [review-reconciler] Accepted pushback on requiring explicit `try/finally` teardown in one-shot local Playwright recording CLI flows. Guidance: do not flag missing defensive cleanup scaffolding by default when the script is intentionally developer-invoked, non-daemon, and scoped to short-lived localhost runs; re-evaluate only if it becomes automation-critical or leak symptoms are observed. Scope: `record-scroll.js` browser/context/page lifecycle.
- 2026-03-06: [review-reconciler] Accepted pushback on pre-validating trusted internal `sites.json` URL fields for local extractor utilities. Guidance: do not flag missing upfront type/shape checks for developer-authored site metadata by default when malformed entries already fail fast in downstream navigation and there is no untrusted input boundary. Scope: `record-scroll.js` site lookup and `page.goto(url)` path.
- 2026-03-06: [review-reconciler] Accepted pushback on concept-summary markdown display priority and freshness checks. Guidance: do not flag `displayFiles` ordering that intentionally prioritizes richer merged `concept.md` over freshly extracted `concept_by_image.md`, and do not require mtime-based freshness selection by default in this local workflow. Scope: `registry.json` concept-summary `displayFiles`, `server/render.js` markdown selection loop in `renderSite`.
- 2026-03-06: [review-reconciler] Accepted pushback on flagging git rename-staging/history handling for generated concept artifacts. Guidance: do not raise findings that require `git mv`/`git add -A` or history-preservation workflows for pre-existing/generated output-file rename sets unless commit behavior introduces a concrete correctness or release risk. Scope: working-tree reviews of `design-system/*/concept-summary` artifact churn.
- 2026-03-06: [review-reconciler] Accepted pushback on treating unrelated pre-existing working-tree deletions as active defects for task-scoped commits. Guidance: when a developer explicitly scopes a commit to specific files and a reported deletion predates the task, do not keep it as an active defect in that task review; track it as separate follow-up only if/when it is included in the commit scope. Scope: dirty-tree reviews with mixed legacy/task-local changes (for example `design-system/*` artifact churn during Task 240-scoped reviews).
- 2026-03-06: [review-reconciler] Accepted pushback on treating non-executed live API calls as active runtime defects when code-level resilience is already present. Guidance: do not keep findings that only assert unverified OpenRouter runtime risk by default when the script already handles timeout/exception/empty-output paths, logs failures, and continues per-site execution; classify live-key verification as task acceptance testing rather than a code defect. Scope: `merge-concepts.js` provider call and per-site error handling loop.
- 2026-03-06: [review-reconciler] Accepted pushback on pre-existing generated-artifact content quality findings in task-scoped reviews. Guidance: when artifact text issues (for example conversational preambles or prior-session tool messages in `design-system/*/concept-summary/*.md`) predate the scoped task and are not part of the intended commit, do not keep them as active defects for that task; track as separate cleanup follow-up only if explicitly requested or included in commit scope. Scope: task-scoped reviews with dirty working trees containing legacy/generated concept-summary artifacts.
- 2026-03-06: [review-reconciler] Accepted pushback on placeholder/video layering in creator gallery cards prior to media rollout. Guidance: do not flag missing `.gallery-card__video` layering/placeholder-swap handling as an active defect when seeded gallery data intentionally uses `videoSrc: null` and no video nodes are rendered at runtime; revisit when non-null media sources are introduced. Scope: `demo/vid-prod/js/gallery.js`, `demo/vid-prod/js/data/creators.js`, `demo/vid-prod/css/components.css`.
- 2026-03-06: [review-reconciler] Accepted pushback on idempotency guards for one-time static gallery initialization. Guidance: do not flag duplicate-filter/timer risks in `initGallery` as defects by default when integration guarantees single invocation on page load in this vanilla static demo flow; revisit if re-mount or repeated-init behavior is introduced. Scope: `demo/vid-prod/public/index.html`, `demo/vid-prod/js/gallery.js`.
- 2026-03-06: [review-reconciler] Accepted pushback on placeholder creator-profile routes during staged vid-prod build. Guidance: do not flag `/creators/:username` card-click navigation as a defect when the task sequence explicitly defers Public Creator Profile implementation to a later step; revisit once that task is in scope. Scope: `demo/vid-prod/public/explore.html`, `project-documentation/task-tracker-config.md`.
- 2026-03-06: [review-reconciler] Accepted pushback on placeholder auth-route redirects in staged vid-prod Explore interactions. Guidance: do not flag `/login.html` redirect targets in Explore follow/auth intercept paths as defects when auth pages are intentionally deferred in the same staged sequence and placeholder links are consistent with current demo routing conventions. Scope: `demo/vid-prod/js/gallery.js`, `demo/vid-prod/public/explore.html`, `project-documentation/task-tracker-config.md`.
- 2026-03-06: [review-reconciler] Accepted pushback on viewport-bottom midpoint trigger interpretation for Explore sticky CTA timing. Guidance: do not flag `rect.top + galleryHeight * 0.5 < window.innerHeight` as incorrect when the intended contract is "show CTA once gallery midpoint crosses viewport bottom"; only escalate if the product requirement changes to absolute document-depth semantics. Scope: `demo/vid-prod/public/explore.html` scroll listener.
- 2026-03-06: [review-reconciler] Accepted pushback on requiring initial sticky-CTA visibility evaluation at page load. Guidance: do not flag missing `updateStickyCta()` invocation on load when the Explore contract explicitly requires CTA reveal only after user scroll-depth interaction and gallery skeleton timing can make load-time threshold checks misleading; continue to accept `scroll`/`resize`-driven updates for this flow. Scope: `demo/vid-prod/public/explore.html` sticky CTA trigger behavior.
- 2026-03-06: [review-reconciler] Accepted pushback on `/public` workspace route prefix in vid-prod app pages. Guidance: do not flag `/public/app/*` and `/public/login.html` links in vid-prod as incorrect when the demo is served from `demo/vid-prod` and public pages are rooted under `/public`. Scope: `demo/vid-prod/public/app/*.html`, vid-prod static-server setups documented in task test plans.
- 2026-03-07: [review-reconciler] Accepted pushback on per-user namespacing for studio tutorial-visited localStorage state in vid-prod app flow. Guidance: do not flag shared `studio_visited` key usage as a defect by default when the page is an intentionally static/local demo and onboarding persistence is specified at demo level rather than account level; revisit if multi-user/browser isolation becomes a requirement. Scope: `demo/vid-prod/public/app/studio.html` tutorial visibility state.
- 2026-03-07: [review-reconciler] Accepted pushback on strict auth-shape validation for unseen-vid localStorage simulation. Guidance: do not flag truthy-object auth gating (`window.__auth`) as a bypass defect by default when the flow is explicitly a local demo auth simulator with no backend enforcement or protected resources and manual tests intentionally use truthy-object toggling. Scope: `demo/unseen-vid/js/shared.js` auth/nav/CTA routing behavior.
- 2026-03-07: [review-reconciler] Accepted pushback on tokenizing every structural layout literal in unseen-vid foundation styles. Guidance: do not flag remaining raw structural/demo scaffolding values as defects by default after core theme constants are tokenized; escalate only when hardcoded values cause concrete drift/regression against intended reusable design tokens. Scope: `demo/unseen-vid/styles/{tokens,base,components}.css` tokenization reviews.
- 2026-03-07: [review-reconciler] Accepted pushback on mobile/responsive and navbar-footer interaction findings for unseen-vid home hero task. Guidance: do not flag missing additional mobile-responsiveness validation or navbar/footer conflict risk as active defects when requirements explicitly defer mobile work and layout uses shared tokens with verified non-conflicting desktop structure; keep only optional manual desktop visual confirmation as acceptance follow-up. Scope: `demo/unseen-vid/index.html`, `demo/unseen-vid/styles/home.css`, `demo/unseen-vid/styles/{tokens,components}.css`.
- 2026-03-07: [review-reconciler] Accepted pushback on strict cubic-bezier derivative correctness in unseen-vid counter animation Newton solve. Guidance: do not flag the current `solveBezierT` slope expression as a defect by default when measured easing/output deviation remains negligible for demo counter animation (sub-pixel/progress-level and non-observable after integer rounding); revisit only if animation requirements demand mathematical exactness or visible timing parity with CSS transitions. Scope: `demo/unseen-vid/js/home.js` counter easing (`solveBezierT`/`easeReveal`).
- 2026-03-07: [review-reconciler] Accepted pushback on requiring storage-API hardening in unseen-vid mock auth submissions. Guidance: do not flag missing `try/catch` around `localStorage`/`sessionStorage` writes and redirect reads in auth submit handlers as a defect by default when the flow is an intentionally local demo with explicit storage-backed auth simulation for design validation; revisit if runtime scope expands beyond local prototype assumptions. Scope: `demo/unseen-vid/js/shared.js` (`handleSignup`, `handleLogin`, `handleOAuthMock`).
- 2026-03-07: [review-reconciler] Accepted pushback on placeholder post-auth redirect targets in staged unseen-vid auth tasks. Guidance: do not flag redirects to intended production routes such as `/app/studio.html` as defects by default when task sequencing explicitly ships auth interception/completion before protected app pages are implemented; re-evaluate once the target-page task is in scope or if the task contract requires fully navigable local end-to-end flow in the same step. Scope: `demo/unseen-vid/js/shared.js` auth submit/OAuth handlers and sequenced unseen-vid task rollouts.
- 2026-03-07: [review-reconciler] Accepted pushback on preserving `concept.md` fallback in concept-summary browser rendering. Guidance: do not flag omission of `concept.md` display fallback as a defect when the UI intentionally presents separate `concept_by_image.md` and `concept_by_video.md` columns and the corpus is migrated; treat `concept.md` as downstream synthesis artifact unless display requirements change. Scope: `server/render.js` concept-summary loading path, `registry.json` concept-summary metadata.
- 2026-03-07: [review-reconciler] Accepted pushback on HTML/script-injection hardening findings for concept-summary markdown rendering in the localhost design browser. Guidance: do not flag markdown-to-HTML self-XSS risks as active defects by default when both content authoring and viewing are intentionally same-user, local-only workflow boundaries; revisit if untrusted or multi-user inputs are introduced. Scope: `server/render.js` concept-summary rendering/editing path.
- 2026-03-07: [review-reconciler] Accepted pushback on unbounded body-size checks for concept video save endpoint. Guidance: do not flag missing payload-size limits on `/api/concept-video/:site` as defects by default in this localhost-only tooling workflow unless deployment exposure broadens or oversized payload failures are observed in practice. Scope: `server.js` concept video POST handler.
- 2026-03-07: [review-reconciler] Accepted pushback on nav token mismatch between computed `elements` and perceptual `visual` color outputs. Guidance: do not flag differences between `elements.nav*` and `visual` nav roles as defects by default when the extractor intentionally preserves raw computed DOM colors alongside LLM-derived perceived colors. Scope: `extractors/color-system.js`, `design-system/*/color-system/colors.{json,css}`.
- 2026-03-07: [review-reconciler] Accepted pushback on replacing fixed nav-state scroll offsets with adaptive probing in local extractor runs. Guidance: do not flag `window.scrollTo(0, 200)` nav-state capture as a defect by default when the extractor targets curated benchmark sites and no concrete mis-capture is demonstrated; revisit only with site-specific evidence that the threshold misses actual nav transitions. Scope: `extractors/color-system.js` nav computed-token and nav screenshot capture passes.
- 2026-03-07: [review-reconciler] Accepted pushback on hard-capping deduplicated button candidate lists in color extraction. Guidance: do not flag missing fixed caps on `buttonCandidates` by default when visual-fingerprint deduplication already keeps observed sets small; revisit only if extraction runs show materially large candidate arrays that impact model reliability or runtime. Scope: `extractors/color-system.js` button candidate scan/prompt payload.
- 2026-03-07: [review-reconciler] Accepted pushback on requiring prompt-schema duplication of computed nav geometry fields. Guidance: do not flag missing `logoWidth`/`logoHeight` entries in the LLM prompt schema as defects when those values are measured programmatically (`getBoundingClientRect`) and preserved by merging computed geometry into top-level `geometry` output. Scope: `extractors/nav-system.js` geometry pass and final return-shape merge.
- 2026-03-07: [review-reconciler] Accepted pushback on single-quote `[role='navigation']` selector matching concern in nav CSS filtering. Guidance: do not flag literal `[role="navigation"]` substring matching as a quote-style defect when selectors are read from CSSOM (`selectorText`/`cssText`), which normalizes attribute quotes to double quotes in this extraction workflow. Scope: `extractors/nav-system.js` `matchesNavSelector` against `document.styleSheets` rules.
- 2026-03-07: [review-reconciler] Accepted pushback on placeholder nav routes in staged goyard-vid nav-component tasks. Guidance: do not flag absolute nav links to not-yet-implemented pages as defects when task scope explicitly builds shared navigation before downstream page implementation in a sequenced task series. Scope: `demo/goyard-vid/partials/nav.html`, task-sequenced goyard-vid frontend work.
- 2026-03-07: [review-reconciler] Accepted pushback on mobile-responsiveness findings for desktop-first goyard-vid nav v1. Guidance: do not flag missing mobile breakpoints/collapsed navigation behavior as defects when task assumptions explicitly state desktop-first delivery and defer hamburger/responsive treatment to a later scoped task. Scope: `demo/goyard-vid/css/nav.css`, goyard-vid v1 nav-component tasks with explicit desktop-only assumptions.
- 2026-03-07: [review-reconciler] Accepted pushback on wordmark href `/` versus `/index.html` consistency in goyard-vid nav. Guidance: do not flag root-path wordmark links as defects by default when the project is served with local static server semantics where `/` and `/index.html` resolve equivalently and no base-path requirement is specified. Scope: `demo/goyard-vid/partials/nav.html`, local static demo routing assumptions.
- 2026-03-07: [review-reconciler] Accepted pushback on `.card-profile-link { visibility: visible; }` inside hidden card overlays for goyard Explore cards. Guidance: do not flag this as an invisible-interaction defect by default when overlay reveal is intentionally driven by `.creator-card:hover` and `.creator-card:focus-within`, preserving keyboard discoverability without observed UX breakage; revisit only if concrete click/focus misfires are demonstrated. Scope: `demo/goyard-vid/css/{gallery.css,pages/explore.css}`, `demo/goyard-vid/explore.html` creator-card overlay links.
- 2026-03-07: [review-reconciler] Accepted pushback on dormant logged-in CTA branch via `window.__auth` hook in goyard creator profile v1. Guidance: do not flag intentionally inactive auth-conditional UI branches as defects when task scope/test plan explicitly validates logged-out default behavior and requires only hidden DOM placeholders for future logged-in state wiring. Scope: `demo/goyard-vid/js/creator-profile.js`, `demo/goyard-vid/creators/username.html`, `project-documentation/scratchpad/task-259/TEST_PLAN.md`.
- 2026-03-07: [review-reconciler] Accepted pushback on automated-test-gap findings for local demo interaction tasks with explicit manual QA plans. Guidance: do not raise missing automated browser-test or "could not assess browser validation" findings by default when project policy and task documentation explicitly prefer manual verification checklists for static/demo flows; only escalate if requirements explicitly demand automated coverage or manual evidence is missing. Scope: goyard-vid Task 259 creator-profile flow (`demo/goyard-vid/creators/username.html`, `demo/goyard-vid/js/creator-profile.js`, `project-documentation/scratchpad/task-259/TEST_PLAN.md`) and similarly scoped local demo tasks.
- 2026-03-07: [review-reconciler] Accepted pushback on CSS attribute-selector escaping for nav href lookup in scroll-video extraction. Guidance: do not flag raw `a[href="${href}"]` interpolation as a defect by default in this local curated-site recording workflow when href values originate from same-page nav anchors and no concrete selector-parse failures are demonstrated; revisit only if extraction logs show malformed-selector misses on real target sites. Scope: `extractors/scroll-video.js` nav exploration element lookup.
- 2026-03-07: [review-reconciler] Accepted pushback on nav scroll no-op finding after transparent-to-solid band transition update. Guidance: do not flag missing visible `.is-scrolled` change when default nav bands are intentionally transparent and scroll state applies opaque tokenized backgrounds matching nav transition intent. Scope: `output/css/layout.css`, `output/css/components.css`, `output/js/nav.js`.
- 2026-03-07: [review-reconciler] Accepted pushback on hidden-default Join CTA text in kinetic nav animation treatment. Guidance: do not flag `nav-cta-btn` inner-text Y-offset + `overflow: hidden` as a defect by default when task requirements explicitly mandate the Lusion-style reveal interaction and planning decisions prioritize source-faithful motion over always-visible static labels. Scope: `output/css/components.css` `.nav-cta-btn`/`.nav-cta-btn span`, `output/nav-component.html`.
- 2026-03-07: [review-reconciler] Accepted pushback on flow re-layout when nav toggles to fixed after scroll threshold. Guidance: do not flag initial in-flow nav (`position: relative`) switching to `.fixed` as a defect when planning decisions explicitly require class-toggle behavior to mirror Hermes source scrolling instead of `position: sticky` stabilization. Scope: `output/css/components.css` `.site-nav`/`.site-nav.fixed`, `output/js/nav.js` scroll toggle.
- 2026-03-07: [review-reconciler] Accepted pushback on replacing nav `data-cta` auth interception with direct auth-page links in output harness nav. Guidance: do not flag anchor `href` navigation to `/login.html` and `/signup.html` as a routing defect when `output/js/auth.js` is explicitly a stub/logging harness and task scope prefers direct destination links. Scope: `output/index.html`, `output/nav-component.html`, `output/js/auth.js`.
- 2026-03-07: [review-reconciler] Accepted pushback on nested-path routing assumptions for flat static output harness pages. Guidance: do not flag relative-vs-root auth link paths as defects by default when the served artifact context is explicitly a flat local static site with no nested route deployment target; revisit only if deployment base-path requirements are introduced. Scope: `output/index.html` hero CTA/auth-link routing reviews in local static harness workflows.
- 2026-03-08: [review-reconciler] Accepted pushback on strict mobile viability findings for explicit 50%-visibility gallery CTA triggers. Guidance: do not flag `IntersectionObserver` `threshold: 0.5` as an active defect when task acceptance criteria explicitly require a 50%-in-viewport trigger; treat mobile-trigger reliability concerns as follow-up scope unless responsiveness is included in the same task contract. Scope: `output/js/gallery.js` `initScrollDepthCTA`, `output/css/gallery.css` responsive gallery layouts.
- 2026-03-08: [review-reconciler] Accepted pushback on gallery skeleton-init no-op findings in placeholder-only stages. Guidance: do not flag `initSkeletonRemoval` as a defect when gallery cards intentionally render without `<img>` elements, the initializer exits cleanly with diagnostic logging, and the function is retained as forward-compatible scaffolding for later media-backed cards. Scope: `output/js/gallery.js` `initSkeletonRemoval`, `output/index.html` placeholder gallery card markup.
- 2026-03-08: [review-reconciler] Accepted pushback on keyboard-only discoverability findings for hover-only gallery overlay links when equivalent signup action is already keyboard-accessible via persistent section/page CTA and per-card overlay links are intentionally decorative/duplicative in this task scope. Guidance: do not flag missing per-card keyboard reveal mechanics as an active defect by default under this contract; revisit if card-specific unique actions are introduced. Scope: `output/index.html` creator-gallery card overlays, `output/css/gallery.css` overlay visibility model, `output/js/gallery.js` hover-state wiring.
- 2026-03-09: [review-reconciler] Accepted pushback on classifying broad tracked artifact deletions as active code defects without explicit task/commit intent. Guidance: when large working-tree deletions are present and no scoped commit intent is provided, request explicit keep-or-restore intent as follow-up instead of treating deletion presence alone as a correctness defect. Scope: repository-wide working-tree reviews with mass artifact churn (for example `design-system/*`, `project-documentation/design-system/*`).
- 2026-03-09: [review-reconciler] Accepted pushback on strict runner-flag exclusivity and queue-mutation hardening findings in local CLI mode handling. Guidance: do not flag non-exclusive mode-flag combinations or transient `--only` queue mutate-then-restore windows as active defects by default when tool scope is single-developer local use, behavior is deterministic/understood, and manual `sites.json` recovery is considered acceptable; revisit only if automation/shared usage requirements are introduced. Scope: `runner.js` flag branch precedence (`--only-one`/`--all`/`--ask-permission`) and `--only` queue restore flow.
- 2026-03-24: [review-reconciler] Accepted pushback on requiring `encodeURIComponent(...)` for editor token page/asset URL segments. Guidance: do not flag missing URI encoding as a defect by default when this workflow enforces ASCII-safe site and asset naming conventions; revisit only if naming rules broaden to include reserved/special URL characters. Scope: `server/editor-tokens.js` URL construction in `enumeratePages`/`enumerateAssets`.
- 2026-03-24: [review-reconciler] Accepted pushback on editor-token asset URL route mismatch during staged task delivery. Guidance: do not flag parser-emitted asset URL shapes as active defects when URL format is explicitly defined by the current task/design doc and server route support is intentionally scheduled for a later task; track as integration follow-up at route-implementation time. Scope: `server/editor-tokens.js` `enumerateAssets` output contract versus deferred server routing task.
- 2026-03-25: [review-reconciler] Accepted pushback on strict API rejection for invalid save-variant override keys/values. Guidance: do not flag silent skip of non-`--*` keys or non-string override values as a defect by default when the endpoint’s caller is trusted/local and skip-on-invalid input is intentional to protect token CSS patch integrity; revisit if caller trust boundaries expand or response contracts require per-key validation reporting. Scope: `server.js` `/api/save-variant` payload handling and `server/save-variant.js` `applyOverridesToCss`.
- 2026-03-25: [review-reconciler] Accepted pushback on assembly-log non-object parse risk in variant save flow. Guidance: do not re-raise `data.variant` TypeError risk when `saveVariant` already coerces parsed `assembly-log.json` values to a plain object fallback (`{}`) before assignment. Scope: `server/save-variant.js` assembly-log parse/coercion block.
- 2026-03-25: [review-reconciler] Accepted pushback on save-variant TOCTOU race/concurrency hardening finding. Guidance: do not flag non-atomic existence-check plus copy/rollback sequences in `/api/save-variant` as active defects by default when this server is scoped to localhost single-user developer workflows and concurrent same-variant requests are outside realistic operating conditions; revisit if the service becomes shared, remote, or automation-concurrent. Scope: `server.js` `/api/save-variant` handler and `server/save-variant.js` destination existence/copy/rollback flow.
- 2026-03-25: [review-reconciler] Accepted pushback on Windows-style backslash traversal hardening for save-variant path segments. Guidance: do not flag missing `\\`/drive-letter path checks as an active defect by default when runtime is explicitly macOS/Darwin local tooling, `variantName` is slug-whitelisted, and the endpoint trust boundary remains single-developer localhost; revisit if cross-platform execution or broader exposure is introduced. Scope: `server/save-variant.js` `isValidSegment`/`isValidVariantName`, `server.js` `/api/save-variant` local workflow assumptions.
- 2026-03-25: [review-reconciler] Accepted pushback on missing staged editor module asset in shell-only milestone delivery. Guidance: do not flag forward-referenced editor script tags (for example `/editor-assets/editor.js`) as active defects when task sequencing explicitly splits shell markup/routes from later client-module implementation and the interim 404 is an acknowledged temporary state. Scope: `server/editor/editor.html` script references and staged editor task rollouts (Task 345 shell, Task 6 client wiring).
- 2026-03-25: [review-reconciler] Accepted pushback on requiring lazy workspace materialization in `/workspace/:site/*` static serving. Guidance: do not flag absence of `ensureWorkspace(...)` invocation in the static workspace file route as a defect when task/spec explicitly assigns workspace creation to a separate `/start` bootstrap flow and the static route is intentionally read-only. Scope: `server.js` `ensureWorkspace`, `/workspace/:site/*` handler, and Delulu-agent bootstrap sequencing contracts.
- 2026-03-25: [review-reconciler] Accepted pushback on strict acknowledgment-token enforcement in agent bootstrap prompts. Guidance: do not flag missing exact-word validation (for example requiring literal `Ready`) as a defect by default when startup completion is intentionally keyed off upstream stream lifecycle (`done`) and the prompt text serves as guidance rather than a protocol contract. Scope: `server.js` `/api/agent/:site/start` context prompt and `drainAgentPrompt` completion behavior.
- 2026-03-25: [review-reconciler] Accepted pushback on stale-agent cache eviction in prompt-proxy route ownership. Guidance: do not flag `/api/agent/:site/prompt` for not clearing/recreating cached agent IDs by default when lifecycle recovery is intentionally centralized in `/api/agent/:site/start` and prompt failures are acceptable in this local dev workflow. Scope: `server.js` `/api/agent/:site/prompt` and `/api/agent/:site/start` lifecycle separation.
- 2026-03-25: [review-reconciler] Accepted pushback on `siteAgents` prototype-key lookup hardening in agent prompt routing. Guidance: do not flag plain-object key lookup (`siteAgents[siteName]`) as an active defect by default when this server is explicitly local-dev scoped, site selection flows originate from known on-disk project names, and manual malformed prompt-route calls are outside the intended trust boundary; revisit if the API is exposed beyond localhost or accepts broader untrusted inputs. Scope: `server.js` `siteAgents` store and `/api/agent/:site/prompt` lookup guard.
- 2026-03-25: [review-reconciler] Accepted pushback on chunk-split SSE parsing timeout risk in agent bootstrap prompt drain. Guidance: do not flag missing cross-chunk line buffering in `drainAgentPrompt` as a false-503/30-second-hang defect by default when upstream prompt streams close naturally after completion and closure is already treated as success; revisit only with concrete evidence of open-but-never-closing streams in this workflow. Scope: `server.js` `drainAgentPrompt` and `/api/agent/:site/start` bootstrap behavior.
- 2026-03-25: [review-reconciler] Accepted pushback on client-disconnect abort enforcement for agent prompt SSE proxying. Guidance: do not flag missing explicit upstream abort wiring on downstream disconnect as an active defect by default when this server is intentionally single-user localhost tooling and transient orphaned prompt streams are accepted tradeoffs; revisit if usage expands to multi-user, shared, or resource-constrained environments. Scope: `server.js` `/api/agent/:site/prompt` upstream fetch/stream pump lifecycle.
- 2026-03-25: [review-reconciler] Accepted pushback on missing sidebar-tab behavior wiring in staged editor shell tasks. Guidance: do not flag inert/new UI shell controls (for example Tokens/Agent tab toggles) as active defects when the current task contract explicitly limits scope to DOM structure/IDs and assigns interactivity wiring to a later task in the same sequence. Scope: `server/editor/editor.html`, `server/editor/editor.css`, staged editor task rollouts with deferred `server/editor/editor.js` wiring.
- 2026-03-25: [review-reconciler] Accepted pushback on keyboard focus-visible styling findings for internal editor-tool shell milestones. Guidance: do not flag missing or minimal focus-ring treatment as an active defect by default in localhost-only single-user developer tooling tasks when accessibility hardening is not part of the acceptance criteria; revisit when accessibility requirements are explicitly in scope or the UI target broadens beyond internal use. Scope: `server/editor/editor.css` editor sidebar tab styling in internal design-tool workflows.
- 2026-03-25: [review-reconciler] Accepted pushback on requiring EOF buffer flush in client-side agent SSE parsing. Guidance: do not flag missing post-loop residual-buffer parse in editor prompt streaming by default when upstream event framing is treated as protocol-conformant in this localhost workflow and stream closure is acceptable completion semantics; revisit only with concrete malformed-framing evidence. Scope: `server/editor/editor.js` `sendPrompt` SSE line parsing.
- 2026-03-25: [review-reconciler] Accepted pushback on duplicate agent-start request hardening in editor tab switching. Guidance: do not flag missing in-flight start guards (for example `agentStarting` or start-button disable) as active defects by default when the editor and agent routes are explicitly localhost-only, single-user development tooling and duplicate starts have no demonstrated corruption/data-loss impact; revisit if tooling becomes shared, remote, or automation-concurrent. Scope: `server/editor/editor.js` `initAgentPanel` start flow and `server.js` `/api/agent/:site/start` local workflow assumptions.
- 2026-03-25: [review-reconciler] Accepted pushback on one-time source URL caching for Tokens/Agent tab restoration in editor panel flow. Guidance: do not flag stale page-tab restoration risk from single-shot `iframe.dataset.sourceUrl` capture as an active defect by default when task/spec intentionally scopes agent-tab behavior to restoring the original pre-agent preview page and does not require page-tab coordination while Agent mode is active; revisit if requirements explicitly add synchronized page-tab state across modes. Scope: `server/editor/editor.js` `initAgentPanel` tab-switch restore logic.
- 2026-03-25: [review-reconciler] Accepted pushback on recommending degraded startup mode when Postgres bootstrap is a declared hard prerequisite. Guidance: do not flag fail-fast-on-DB-init as a defect by default when the tool contract requires Postgres for normal operation and startup now emits explicit operator guidance before exiting. Scope: `server.js` `initDb()` startup gate and local dev bootstrap expectations.
