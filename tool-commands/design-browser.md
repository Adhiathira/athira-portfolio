# Design System Browser

Local HTTP server for browsing and inspecting extracted design tokens per site.

## Run

```bash
# Production (no auto-restart)
npm run browse

# Dev mode (auto-restarts on file changes)
npm run dev
```

URL: http://localhost:5509
