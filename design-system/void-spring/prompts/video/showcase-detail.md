# Showcase Detail — CLI Trace Output — Video

**File name:** showcase-detail.mp4
**Save to:** `void-spring/assets/showcase-detail.mp4`
**Type:** Video
**Aspect ratio:** 3:2
**Duration:** 8s (loopable)
**AI Tool:** Kling 2.6

## Prompt

A frameless terminal window against a near-black background running the command `nullfield trace --live`. The camera holds still. New log lines scroll upward continuously — a live stream of routing decisions formatted as a structured table. Timestamps appear in muted grey, route names in light grey, latency values flash in electric pink-red when they exceed threshold and in green for optimal paths. The scroll is steady and unhurried, like a real production system under moderate load. Roughly every two seconds, a new anomalous line appears highlighted with a subtle pink-red glow background, drawing the eye. The terminal is dense and fills the frame to the edges — pure monospace text, zero UI chrome. The scroll is continuous and seamlessly loops back to the same state at the end of the clip.
