# Spotlight — Export Code Panel

**File:** assets/spotlight-export-code.jpeg
**Save to:** `landing-page/assets/spotlight-export-code.jpeg`
**Type:** Image
**Dimensions:** 960×720
**Aspect ratio:** 4:3
**AI Tool:** Higgsfield Nano Banana Pro
**Page:** index.html
**Section:** #feature-spotlight

## Context from code
- **Alt text:** "Volt Studio export panel showing clean HTML and JavaScript code output on a dark code editor interface"
- **Nearest heading:** "Design once. Ship everywhere."
- **Section purpose:** Feature spotlight showing the export pipeline's clean code output
- **Background:** Dark #0f0f10 code editor, slightly raised from #19191c page bg

## Prompt

A professional UI screenshot of a code export panel inside a dark design tool. The main content area shows a syntax-highlighted code editor displaying clean HTML5 markup — a hero section div with CSS custom properties like `--color-page-bg: #19191c` and `--ease-standard: cubic-bezier(0.4, 0, 0.2, 1)`. Below the HTML, a JavaScript section shows a compact GSAP animation setup, roughly 12 lines, with `gsap.from('#hero', {...})` call. The syntax highlighting uses: keywords in #fd356e pink-red, strings in soft green #a3e635, comments in dim #555560, property names in lavender #c084fc, numbers in amber #f59e0b. The panel header bar shows "Export Ready" label with a green checkmark and "2.3kb" bundle size badge. The code editor has line numbers on the left in dim grey. No visible scrollbars. The overall look is a premium, minimal developer tool — precise, clean, professional. Natural cast shadow beneath the panel with soft depth-of-field.

## Usage

Used in `index.html` inside `#feature-spotlight` as the right-column feature image showing the export pipeline output.
HTML: `<img id="spotlight-img" src="../assets/spotlight-export-code.jpeg" alt="Volt Studio export panel showing clean HTML and JavaScript code output on a dark code editor interface">`
