{
  "_source": "slite",
  "_twist": "Snappier hover transitions (twist A, intensity 3): all hover durations tightened by ~100ms and clamped at 50ms min — button 200→100ms, link 250→150ms, navLink 130→50ms. Crisp, decisive B2B SaaS response. No color changes, no box-shadow, no scale.",
  "hover": {
    "button": {
      "letterSpacing": "0.02em",
      "transition": "letter-spacing 100ms ease-out",
      "_note": "Subtle tracking expansion — the type loosens as you approach, no color shift"
    },
    "link": {
      "textDecoration": "underline via ::after pseudo-element",
      "underlineAnimation": {
        "transform": "scaleX(0) → scaleX(1)",
        "transformOrigin": "left center",
        "duration": "150ms",
        "easing": "ease-out"
      },
      "_note": "No color change on link hover — underline is the only indicator"
    },
    "navLink": {
      "textDecoration": "underline via ::after pseudo-element",
      "underlineAnimation": {
        "transform": "scaleX(0) → scaleX(1)",
        "transformOrigin": "left center",
        "duration": "50ms",
        "easing": "ease-out"
      }
    }
  },
  "focus": {
    "outline": "1px solid",
    "outlineColor": "var(--color-accent)",
    "outlineOffset": "3px",
    "borderRadius": "0px",
    "_note": "Minimum accessible focus — matches hairline weight of structural dividers"
  },
  "active": {
    "opacity": "0.75",
    "transform": "none",
    "_note": "Active state via opacity only — no scale, no color shift"
  },
  "disabled": {
    "opacity": "0.35",
    "cursor": "not-allowed",
    "pointerEvents": "none"
  },
  "visited": {
    "color": "inherit",
    "_note": "Visited links retain near-black — no secondary text color tier"
  },
  "rules": {
    "noBoxShadow": true,
    "noColorChangeOnHover": true,
    "noScaleTransforms": true,
    "_note": "Typographic mass is stable under interaction — weight register does not shift, scale, or animate in response to cursor events"
  }
}
