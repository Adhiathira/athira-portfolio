`★ Insight ─────────────────────────────────────`
This task is pure text transformation — no code, no tools needed. The goal is semantic substitution: replace proper nouns and domain-specific terms while leaving structural/spatial language untouched. The tricky cases are color names embedded in design descriptions (e.g., "white/light background" is already generic and safe to keep) and brand-specific UI copy inside quotes (CTAs need genericizing).
`─────────────────────────────────────────────────`

# Brand Name — Layout Brief

---

## 1. Overall Page Structure

The page opens with a full-viewport modal (cookie consent) overlaying a partially visible homepage. From what's visible beneath, the structure follows a classic editorial format: a fixed header anchored at the top, followed by a full-bleed hero image. The overall flow is top-down, cinematic, and unhurried — consistent with the brand's prestige positioning. Content rhythm appears generous with substantial breathing room between elements.

---

## 2. Header / Navigation Layout

The header is a two-tier system:

**Top utility bar** — a slim, understated strip sitting flush to the top edge. Left-aligned text links ("Region," "Contact Us," "Services") in very small uppercase. The Brand Name wordmark/logo is centered, rendered in an elegant serif script — it commands visual authority without competing with the utility items. Right-aligned icon cluster: heart (wishlist), person (account), location pin, shopping bag — four utility icons evenly spaced.

**Primary navigation bar** — a second horizontal strip immediately below. Seven navigation categories in small uppercase representing core product and content areas. Items are evenly distributed across the full width. A thin vertical rule separates the last nav item from a search icon on the far right. The overall typographic scale is deliberately restrained — hierarchy comes from position and spacing, not size.

The two-tier structure creates a clear separation between utility (top) and product navigation (bottom). Both tiers appear to be full-width, light background, with no visible dividers between the two bands — they read as a unified header block.

---

## 3. Hero Section

Partially visible beneath the cookie modal, the hero appears to be a **full-bleed, full-viewport image** — edge-to-edge, no padding or container constraints. A subject is photographed in high contrast, cinematic lighting against a textured dark background. The image fills the entire viewport width and bleeds to the bottom of the screen.

This is a classic editorial approach: the product story is told through atmosphere and imagery, not copy-heavy layouts. Any headline or CTA would likely be overlaid directly on the image, centered or lower-third positioned.

---

## 4. Content Sections Below Hero

Not visible in these screenshots — the cookie modal blocks all content below the hero fold. Based on the brand's established design language, subsequent sections would typically follow editorial grid patterns (asymmetric two-column, product card grids, full-bleed editorial moments alternating with contained product layouts), but this cannot be confirmed from the provided screenshots.

---

## 5. Footer Layout

Not visible in these screenshots.

---

## 6. Notable Layout Patterns

**Cookie Modal — Centered Overlay:**
The consent modal is the dominant element across all screenshots. It sits center-screen using a classic lightbox pattern — fixed position, horizontally and vertically centered, with sufficient top/bottom margin to show the underlying page behind it. The modal itself has a clean light background, substantial internal padding, and generous line spacing in the body copy. The three CTA buttons ("Primary CTA," "Configure Preferences," "Minimal Option") are arranged in a horizontal row at the bottom, each rendered as an equal-width outlined/filled button in uppercase — the primary action uses a filled/inverted treatment to establish hierarchy.

**Two-tier header with split utility:**
The separation of utility navigation (localization, account, cart) from product navigation is a deliberate hierarchy choice — it keeps commerce tools spatially distinct from brand storytelling.

**Full-bleed hero as backdrop:**
Even with the modal present, the hero image bleeds to all four edges of the viewport, reinforcing the immersive, editorial sensibility that defines premium digital experiences.

**Spatial restraint as a design tool:**
Throughout the header, the typographic scale is intentionally small and sparse. Whitespace does the heavy lifting. This is a deliberate proportional choice — the restraint signals prestige and lets the imagery and logo carry weight.